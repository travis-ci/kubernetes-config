apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "travis-vcs.fullname" . }}-test-connection"
  labels:
{{ include "travis-vcs.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "travis-vcs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
