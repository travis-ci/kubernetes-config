apiVersion: v1
kind: Service
metadata:
  name: {{ include "shield.fullname" . }}
  labels:
    {{- include "shield.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shield.selectorLabels" . | nindent 4 }}
