apiVersion: v1
kind: Service
metadata:
  name: {{ include "travis-gatekeeper.fullname" . }}
  labels:
    {{- include "travis-gatekeeper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "travis-gatekeeper.selectorLabels" . | nindent 4 }}
