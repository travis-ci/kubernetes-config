apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "travis-gatekeeper.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "travis-gatekeeper.name" . }}
    helm.sh/chart: {{ include "travis-gatekeeper.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "travis-gatekeeper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
